export const metadata = {
  title: "Metadata Generator",
  description: "Extract SEO-optimized titles and keywords from images using AI vision models. Batch processing supported.",
  openGraph: {
    title: "Metadata Generator",
    description: "Extract SEO-optimized metadata from images using AI vision models.",
  },
};

export default function Layout({ children }) {
  return children;
}
