export const metadata = {
  title: "Prompt History",
  description: "View, search, and manage your generated prompt history across all content types.",
  openGraph: {
    title: "Prompt History",
    description: "Browse and reuse your previously generated AI prompts.",
  },
};

export default function Layout({ children }) {
  return children;
}
